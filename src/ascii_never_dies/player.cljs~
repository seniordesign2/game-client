(ns ascii-never-dies.player)

;; player vars
(def pos (atom 0))

;; initializes player position to center board
(defn init-player-pos [height width]
  (swap! pos + (* width (/ height 2)) (/ width 2)))
